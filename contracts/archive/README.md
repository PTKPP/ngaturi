# TASK-002 Contract Archive

`openapi-task-002.yaml` and `examples/` preserve the contract history used for the completed backend authentication phase and the earlier API-first plan.

These files are not used by the frontend-first workflow, are not a source of truth for new features, and are excluded from the active Chroma knowledge index. Do not extend them for frontend tasks.
